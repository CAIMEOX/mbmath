name = "CAIMEOX/moon_floating"

version = "0.3.1"

readme = "README.md"

repository = "https://github.com/CAIMEOX/mbmath.git"

license = "Apache-2.0"

keywords = [ "arbitrary-precision", "floating-point", "math" ]

description = "Arbitrary-precision real, complex, interval, and numerical computing for MoonBit"
